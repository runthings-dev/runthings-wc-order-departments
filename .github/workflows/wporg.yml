name: Deploy to WP.org

on:
  push:
    tags:
      - "v*"

jobs:
  deploy:
    uses: runthings-dev/.github/.github/workflows/wporg-svn-deploy.yml@master
    secrets:
      WPORG_USERNAME: ${{ secrets.WPORG_USERNAME }}
      WPORG_PASSWORD: ${{ secrets.WPORG_PASSWORD }}